# Optional. Default remote PostgreSQL URI for collaboration (any provider).
# Copy to `.env` and fill in. Never commit real credentials.
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
